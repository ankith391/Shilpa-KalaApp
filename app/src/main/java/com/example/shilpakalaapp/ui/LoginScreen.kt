package com.example.shilpakalaapp.ui

// This file is deprecated. Please use AuthScreen.kt for authentication.
import androidx.compose.runtime.Composable

@Composable
fun LoginScreen(viewModel: MainViewModel, onLoginSuccess: () -> Unit) {
    // Dummy implementation to avoid build errors.
    // The app now uses AuthScreen instead.
}
